@model IEnumerable<QRModel>

<div class="container">
    <div class="row">

        @foreach (var qr in Model)
        {
            <div class="col-md-2">
                <partial name="_tagThumbnail" model="@qr" />
            </div>
        }

    </div>
</div>
